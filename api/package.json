{
  "name": "clarmory-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute DB --local --file=schema.sql && wrangler d1 execute DB --local --file=scripts/seed.sql"
  },
  "devDependencies": {
    "wrangler": "^4.80.0",
    "vitest": "^3.2.1",
    "@cloudflare/vitest-pool-workers": "^0.8.32",
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.8.3"
  }
}
